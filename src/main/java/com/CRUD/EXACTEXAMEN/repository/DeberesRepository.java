package com.CRUD.EXACTEXAMEN.repository;

import com.CRUD.EXACTEXAMEN.model.Deberes;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DeberesRepository extends JpaRepository<Deberes,Long> {
}
